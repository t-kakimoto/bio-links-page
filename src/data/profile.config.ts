import type { SiteProfile } from '../types/config';

export const PROFILE_CONFIG: SiteProfile = {
  name: '柿元崇利 / Takatoshi Kakimoto',
  bio: '',
  avatarUrl: `${import.meta.env.BASE_URL}profile-avatar.jpg`,
};
